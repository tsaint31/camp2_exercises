// ## Decreasing Iteration on arrays
//
// -  Create a function reversePrintArrayEntry that write on `stdout` each values of the `litteralDigits` array, descending.
// Hint: if a parameter is not passed to a function, you can recall the function itself with a default parameter

const litteralDigits = ['zero', 'one', 'two', 'three', 'four', 'five',  'six', 'seven', 'eight', 'nine'];

function reversePrintArrayEntry(array, index) {
  // Your code here
}

reversePrintArrayEntry(litteralDigits); // this call should display each entry of the array in reverse order

// do not remove this line, it is for tests
module.exports = reversePrintArrayEntry;

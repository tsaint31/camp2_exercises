// ## Iteration on arrays
//
// -  Create a function printArrayEntry that will print each entry on stdout

const litteralDigits = ['zero', 'one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine'];

function printArrayEntry(array, index) {
  // Your code here
}

printArrayEntry(litteralDigits, 0); // this call should display each entry of the array

// do not remove this line, it is for tests
module.exports = printArrayEntry;

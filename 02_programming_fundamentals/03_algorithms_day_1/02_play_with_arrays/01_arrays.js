/*
 ## Define arrays and access to their values
 */

// Create an array called `digits` with the number from 0 to 9;


// create a variable called `first` for the first cell of your array;


// create a variable called `last` for the last cell of your array;


// create a variable called `sixth` for the cell at the position 6 of your array;
